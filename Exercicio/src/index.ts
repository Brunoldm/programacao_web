function lista(numero01 : number, numero02: number, numero03: number, numero04: number ): number {
    let resultado;
    switch (operacao) {
        case "soma": {
             max(numero01,numero02,numero03,numero04);fff

}
}
